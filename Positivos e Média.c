#include <stdio.h>

int main(){
		int val,soma,cont;
		scanf ("%d", &val);
		for (cont=1;cont<=val;cont++){
			if (val>=0){
			printf ("%d\n", cont);
				cont++;}
		}
		return 0;
		
		
}
